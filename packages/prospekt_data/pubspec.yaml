name: prospekt_data
description: Data implementations (Drift, Parsers) for Prospekt Essens Planer.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.11.3
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  prospekt_core:
    path: ../prospekt_core
  drift: ^2.20.0
  sqlite3_flutter_libs: ^0.6.0
  path_provider: ^2.1.3
  path: ^1.9.0
  google_mlkit_text_recognition: ^0.13.0
  meta: ^1.11.0
  pdfrx: ^2.2.24

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  drift_dev: ^2.20.0
  build_runner: ^2.4.9
